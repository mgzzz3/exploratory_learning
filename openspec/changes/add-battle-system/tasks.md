## 1. 数据模型与迁移

- [x] 1.1 新增 `battle_rooms` / `battle_participants` / `battle_answers` SQLAlchemy 模型（含状态枚举、`(participant_id, level_position)` 唯一约束、房间-参与者外键），并验证模型可被导入且约束定义正确
- [x] 1.2 新增 Alembic 迁移创建三张对战表并验证迁移在测试库上 up/down 均成功，现有表无变更

## 2. 对战服务层

- [x] 2.1 实现建房服务：创建房间、后台触发题目生成（复用现有生成策略与持久化）、立即可分享；生成失败进入错误态；验证建房后房间处于 generating 且生成完成后进入 waiting
- [x] 2.2 实现加入服务：校验房间状态、容量上限 2、禁止房主加入自己房间；验证满员与房主自加入均被拒绝且错误码稳定
- [x] 2.3 实现就绪与起点服务：双方就绪时在行锁下写入 `started_at` 与 `expires_at`（+3 分钟），仅此一次；验证重复就绪不重置起点、未齐前无法获取题目
- [x] 2.4 实现对战答题服务：校验参与者与 playing 状态后服务端判定对错，无论对错推进题号，不触碰脑力/复活/助攻/用户统计；唯一约束兜底重复提交并返回首次结果；验证答错前进与幂等测试通过
- [x] 2.5 实现惰性结算服务：在提交末题与查询房间状态两个入口检查"双方完成"或"已过 `expires_at`"，行锁下一次性结算；正常结算按对题数、总用时、平局排序，超时结算按对题数、完成者优先、双方零记录作废；验证全部结算分支测试通过
- [x] 2.6 实现结果与复盘服务：仅当本人已完成作答且房间已结算时返回逐题"本人选择 + 正确答案 + 解释"与胜负；验证作答中请求复盘被拒绝且任何未结算响应不含正确答案
- [x] 2.7 实现等待期作废：题目就绪 3 分钟无人加入则房间作废且无胜负；验证过期判定与作废测试通过

## 3. API 路由与契约

- [x] 3.1 新增对战 Pydantic schema（建房、加入、就绪、房间状态、提交答案、结果复盘），确保对战期间与未结算响应 MUST NOT 含 `correct_option`；验证 schema 测试通过
- [x] 3.2 新增对战路由并挂载到应用（建房、加入、就绪、状态查询、提交答案、结果获取，均要求登录态）；验证路由鉴权与不存在房间的 404 行为

## 4. 后端测试与集成

- [x] 4.1 编写房间生命周期集成测试（generating -> waiting -> playing -> finished / void，含生成失败态），验证与迁移和真实持久化的端到端流转
- [x] 4.2 编写答案安全测试：作答与状态响应不泄露正确答案、复盘仅在完成后可见，验证响应字段断言全部通过
- [x] 4.3 编写结算专项测试（对题数优先、同分比用时、完全平局、超时完成者优先、双方零记录作废）与并发结算只生效一次的测试，验证全部通过
- [x] 4.4 运行后端完整测试套件并确认无回归

## 5. 前端服务与状态

- [x] 5.1 新增对战 API client 与类型（建房、加入、就绪、状态、提交、结果），复用现有 request 封装与登录保障；验证服务单测通过
- [x] 5.2 新增对战 Zustand store（当前房间、双方状态、我的题号与结果缓存）；验证状态流转单测通过

## 6. 前端页面

- [x] 6.1 实现建房/等待页：关键词输入、生成中/等待中/错误态、邀请卡片分享（卡片 path 携带房间 id），验证各状态渲染与分享参数正确
- [x] 6.2 实现受邀加入页：从卡片参数进入、登录后加入、满员/过期/生成失败错误提示，验证各分支页面行为
- [x] 6.3 实现就绪与倒计时页：双方就绪状态展示、服务端 started_at 后的 3-2-1 纯演出倒计时，验证倒计时结束进入作答且不发额外计时请求
- [x] 6.4 实现对战作答页：题目展示与选项提交、对/错轻反馈后自动前进、不渲染脑力/复活/助攻，完成三题后进入结果页；验证答错前进与完成跳转
- [x] 6.5 实现结果页：等待对方时 3 秒轮询房间状态，结算后展示胜负、双方对题数与用时、逐题复盘（含答错题正确答案与解释）；验证两态切换与复盘数据渲染

## 7. 整体验证与文档

- [x] 7.1 运行前端完整测试与 lint，修复本变更引入的问题
- [ ] 7.2 端到端手工验证：建房 -> 分享 -> 加入 -> 双方就绪 -> 各自作答 -> 结算 -> 双方结果页一致；验证一人中途退出时超时结算正确
- [x] 7.3 运行 `openspec validate add-battle-system --strict` 并确认通过
