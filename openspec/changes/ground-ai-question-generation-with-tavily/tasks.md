本次修订以已确认的 `proposal.md`、`specs/grounded-game-generation/spec.md` 和 `design.md` 为准。已勾选任务及其编号保留为此前版本的进展记录，不代表新增设计已经实现；未完成项按依赖重新拆分、排序，所有新增项均未勾选。后端新增或修改行为必须先运行针对性测试、确认预期失败，再实现并运行通过；验收记录保留测试命令及红/绿结果，不记录密钥或原始研究内容。

原 1.3 的配置不能证明端到端预算已生效，须经 1.9–1.10、5.7–5.8 重新验收；原 4.2 的 `ResearchBundle` 输出目标由 2.7–2.8、4.8–4.13 的服务端台账与 `ResearchConclusion` 替代；原 5.3/5.6 的通过记录不能替代本次错误透传、许可及持久化测试。原 6.1 的原型确认只覆盖旧联网状态，新增 UI 必须通过 6.4 的人工确认。原未完成 MySQL 任务 5.7 移至 5.22，前端与交付未完成任务在第 6、7 组重新编排。

本清单只规定后续实施，不授权在规划修订时开发、改动真实环境或部署。自动化测试使用 fake/mock；真实接口回归单列于 7.6–7.8，限定开发环境。生产发布、主规格同步和归档仍在实施验收后按对应流程另行执行。

## 1. 凭证、官方依赖与配置基线

- [x] 1.1 在 Tavily 控制台轮换已通过普通文本出现的旧凭证，并仅将新值注入仓库根 `.env` 与服务器 `backend/.env.production`；通过不输出值的存在性检查确认两个运行环境均有 `TAVILY_API_KEY`，并确认旧 key 已失效
- [x] 1.2 先为研究配置编写失败测试，覆盖真实服务模式缺 Tavily/DeepSeek key、mock 模式无 key、`SecretStr` 序列化与日志脱敏、Agent/工具/字符/超时硬边界；运行目标 `pytest` 确认测试因配置尚未实现而按预期失败
- [x] 1.3 在 `Settings` 中实现 Search 8 秒、Extract basic 12 秒/advanced 25 秒、瞬时错误重试一次、Agent 最多 4 次工具调用/6 次模型调用、85 秒后端总预算与其他上限；在 `.env.example` 只增加空的 `TAVILY_API_KEY=` 占位，运行 1.2 测试确认全部通过
- [x] 1.4 按官方文档向 `backend/pyproject.toml` 增加并锁定兼容 Python 3.11 的 `langchain`、`langchain-deepseek`、`langchain-tavily`，更新依赖环境；通过导入 `create_agent`、`ToolStrategy`、`ChatDeepSeek`、`TavilySearch` 和 `TavilyExtract` 以及现有 packaging 测试确认没有使用旧 `langchain_community` Tavily 集成
- [x] 1.5 先写模型能力检查的失败测试，再实现真实模式启动/smoke-test 检查，确认所配置 DeepSeek 模型支持 Tool Calls 与结构化输出；能力不满足时必须立即失败且不得静默切换模型
- [x] 1.6 先为 `QUESTION_GENERATION_MODE=grounded|legacy` 编写失败配置测试，覆盖默认 grounded、非法值、grounded 缺 Tavily/DeepSeek key、legacy 只要求旧 DeepSeek 配置、mock 两种模式和 `SecretStr` 脱敏；实现按策略校验后运行测试，确认 legacy 不读取 Tavily key 且不执行研究模型能力检查
- [x] 1.7 先为可选依赖与延迟导入编写失败 packaging 测试，再把 `langchain`、`langchain-deepseek`、`langchain-tavily` 移入 `research` extra；分别在不安装 extra 的全新环境验证 legacy 可启动/创建关键词游戏，以及安装 `.[research,test]` 后官方五个导入与 grounded 启动通过
- [x] 1.8 在实施前复核 `design.md` 所列 LangChain 中间件/结构化输出、ChatDeepSeek、Tavily Search/Extract、Python 截止时间、PyJWT 与 Taro 官方文档；对照本机实际依赖版本确认动态工具筛选、有限 `handle_errors`、语言参数透传和异步 hook，记录文档链接、核查日期及依赖版本。以离线导入/接口契约检查通过为完成标准，不为核查读取或输出真实 key，也不擅自更换模型
- [x] 1.9 先为统一预算配置补写失败测试：总预算最多 85 秒，生成校验预留默认 40 秒、研究整理预留默认 15 秒、必需校验预留默认 15 秒，覆盖零/负值、预留关系不成立、缩短总预算及工具 4/Search 2/Extract 2/模型 6 的硬上限；运行目标 `pytest` 确认当前缺失行为按预期失败
- [x] 1.10 实现 `RESEARCH_GENERATION_RESERVE_SECONDS`、`RESEARCH_FINALIZATION_RESERVE_SECONDS`、`GROUNDING_VALIDATION_RESERVE_SECONDS` 及与 `RESEARCH_TOTAL_TIMEOUT_SECONDS` 的关系校验，只更新不含秘密的环境示例；运行 1.9 和既有配置/legacy 隔离测试，确认默认形成 30 秒检索、45 秒研究截止及 85 秒总截止，非法组合不能启动

## 2. 输入、安全与证据领域模型（TDD）

- [x] 2.1 先为输入分类与长度编写失败测试，覆盖普通关键词、含点号的关键词、合法 HTTP(S) URL、关键词 80 字符边界、URL 2048 字符边界、多个 URL 和 URL/文本混合输入；运行目标测试确认因分类器尚未实现而失败
- [x] 2.2 实现 `InputDescriptor` 与权威后端分类：仅当完整输入可解析为单个 HTTP(S) URL 时进入 URL 模式，否则按关键词规则处理；运行 2.1 测试确认边界行为通过且现有关键词请求保持兼容
- [x] 2.3 先为 URL 安全与规范化编写失败测试，覆盖用户凭证、非 HTTP(S)、localhost、IPv4/IPv6 私网/链路本地、混淆 IP、DNS 解析到私网、重定向到不安全目标、敏感/追踪查询参数和片段；运行目标测试确认失败
- [x] 2.4 实现可注入 DNS fake 的 URL 安全校验和规范化；后端不直接跟随重定向，并对 Tavily 响应中可见的最终来源 URL 再次检查，无法确认公开目标时失败关闭；运行 2.3 测试确认不安全目标返回 `INVALID_SOURCE_URL`，日志与持久化内容不含用户凭证或敏感查询参数
- [x] 2.5 先为 `SearchResult`、`ExtractedPage`、`SourceReference`、`ToolCallRecord`、`ResearchBundle/ResearchContext`、`GroundedGeneratedGame` 和 `GroundingReport` 编写失败测试，覆盖严格额外字段、`search|extract` 获取方式、关键词 2～5/URL 1～5 来源、原始页面必含、关卡来源 ID 及三关既有约束
- [x] 2.6 实现 2.5 的 Pydantic v2 模型与校验器；运行目标测试与现有 `test_generated_schema.py`，确认有效样例通过、非法来源/调用轨迹被拒绝且既有三关约束未回归
- [x] 2.7 先为 `ResearchConclusion` 和请求级 `ResearchRunState`/证据台账写失败测试，覆盖严格字段、结论仅引用已取得来源、服务端补齐 URL/检索时间/调用 ID/耗时、未执行或失败调用不能充当证据、关键词 2～5 与 URL 1～5 来源及原页面必含；运行目标测试确认新契约尚未实现而失败
- [x] 2.8 实现精简结论模型和服务端台账到 `ResearchBundle/ResearchContext` 的组装，工具成功后立即登记稳定来源 ID 与证据块，不要求模型复写可信元数据；运行 2.7、既有证据模型和生成 Schema 测试，确认伪造来源被拒绝、完整正文不进入最终模型结果或持久化数据

## 3. Tavily Search/Extract 适配工具（TDD）

- [x] 3.1 先为 `adaptive_tavily_search` 编写失败契约测试，使用记录参数的 fake 断言动态 `query/content_mode/search_depth/max_results/topic/time/date/domains/country/language`、摘要关闭 raw、全文使用 Markdown、禁用 answer/images/auto parameters、异步调用、超时和错误分类
- [x] 3.2 使用官方 `langchain_tavily.TavilySearch` 实现类型化 Search 适配工具：每次调用按 Agent 参数构造受限实例并规范化结果；运行 3.1 测试确认 `max_results≤8`、全文最多 3 条、`country` 只用于 general、认证/参数错误不重试、瞬时故障仅重试一次
- [x] 3.3 先为 `adaptive_tavily_extract` 编写失败契约测试，断言 1～3 个 URL、`basic|advanced`、整页模式不传 `query`、补读模式允许相关性 `query`、Markdown 输出、失败 URL、超时、URL 二次安全检查和错误分类
- [x] 3.4 使用官方 `langchain_tavily.TavilyExtract` 实现类型化 Extract 适配工具并规范化正文/失败/用量；运行 3.3 测试确认用户直达 URL 首次调用只抽取原 URL，且不可读页面稳定映射为 `PAGE_UNREADABLE`
- [x] 3.5 先为整页临时存储与分块编写失败测试，覆盖标题/段落边界、单页 120,000 字符处理上限、40,000 字符模型上下文上限、章节索引、相关块选择、释放临时正文和“不得只截页面开头”；实现后运行测试确认超大页面返回 `PAGE_UNREADABLE`，预算内正文不进入数据库、日志或 Agent 最终结构化结果
- [x] 3.6 先扩展官方工具适配契约的失败测试，用受控 HTTP/工具 fake 覆盖认证、限流、网络、单次超时、服务不可用、错误载荷和空/超大/不支持页面；断言安全 reason、逻辑调用与物理请求计数、实际等待不超过剩余检索窗口、瞬时网络最多重试一次且退避计时；补齐已确认的官方固定错误格式数字状态码解析、结构化状态优先及未知格式失败关闭测试。运行测试确认现有错误或预算缺口按预期暴露
- [x] 3.7 实现 Search/Extract 类型化错误与剩余窗口内的有限重试，不把官方工具返回的错误载荷当正常资料；仅在官方适配边界按设计第 12 节兼容固定格式的数字 HTTP 状态码，不按自由文案猜测；运行 3.6 及全部既有适配测试，确认 `SEARCH_UNAVAILABLE`/`PAGE_UNREADABLE` 分类保留，认证/限流/参数/响应结构错误不循环重试，动态参数仍经官方包传送且无敏感原文进入异常详情

## 4. 单一研究 Agent 与动态参数策略（TDD）

- [x] 4.1 先为 Agent 编排编写失败测试，覆盖关键词必须调用 Search、URL 必须先对原页面调用 Extract、`Search→Extract`、`Extract→Search`、一次工具后停止、零工具/越权工具/伪造调用引用、无效结构化输出与预算超限；运行目标测试确认失败
- [x] 4.2 用 `ChatDeepSeek`、`create_agent`、两个适配工具和 `ToolStrategy(ResearchBundle)` 实现单一研究 Agent；加入真实调用轨迹验收、工具白名单、最多 4 次工具/6 次模型调用与 85 秒总预算，运行 4.1 测试确认违规分支重试后返回 `RESEARCH_AGENT_FAILED`
- [x] 4.3 先以参数化测试覆盖动态策略：简单概念 basic/fast 摘要 3 条、一般概念摘要 5 条、新知识 advanced 5～8 条并按需全文、摘要不足后 Extract、新闻时间范围、中文国家增强、城市写入 query、国际主题补英文且默认 `filter_by_language=false`
- [x] 4.4 完善 Agent 工具描述与系统提示并实现适配器硬边界；运行 4.3 测试确认 Agent 可自主选择参数，但不存在 city 伪参数、`country` 不误用于非 general、`auto_parameters` 始终关闭且语言默认只作排序增强
- [x] 4.5 先为外部内容提示注入编写失败测试，覆盖网页/搜索正文要求泄露密钥、改写工具、跳过联网、改变角色或输出 Schema；实现不可信内容边界与服务端后置检查，确认所有注入指令无效且不会把 OpenID/JWT/用户资料加入工具参数
- [x] 4.6 先为统一 `QuestionGenerationStrategy`、策略工厂和可持久化结果编写失败测试，覆盖服务器配置是唯一模式来源、请求额外模式字段被拒绝、两种策略共享写入契约，以及 factory 只构造选中策略
- [x] 4.7 实现 `LegacyGenerationStrategy` 与延迟导入的策略工厂：关键词只调用一次原 `ContentGenerator.generate(topic)`，URL 在生成前返回 `URL_REQUIRES_RESEARCH`，结果固定 `input_type=keyword/retrieved_at=null/sources=[]/source_ids=[]`；运行 4.6 测试并确认 Tavily、LangChain Agent、研究能力检查和 grounded fake 均为零调用
- [x] 4.8 先使用真实 `create_agent`、可脚本化模型 fake 和工具 fake 写编排失败测试，覆盖关键词 Search、URL 原页面 Extract、`response_format=ToolStrategy(ResearchConclusion, ...)` 的 `structured_response`、异步中间件实际被执行及研究客户端非思考/禁用隐式重试配置；运行目标测试确认当前自然语言 JSON 截取或装配缺口失败，不能仅以预制 Agent 返回字典代替该测试
- [x] 4.9 按官方接口实现请求内一次 Agent 装配与结构化结论接收，研究使用配置的模型和明确的非思考参数，保持 Responses 生成客户端与 legacy 配置隔离；运行 4.8 及原编排测试，确认结论通过台账组装、响应工具不算第三个联网工具、模型调用仍计入同一额度
- [x] 4.10 先写收敛失败测试：一次摘要已足够则停止、两次 Search 加一次 Extract 后第三次 Search 意图被阻止但能用已有资料完成、四次工具后转整理、只剩两次模型额度时保留结论/纠错、同轮并发工具不能超额；分别断言外部调用数、真实证据保留、未执行反馈不记成功和无法形成合格证据时失败，运行测试记录预期红灯
- [x] 4.11 实现按请求阶段/预算动态筛选工具与执行边界原子预占，超限意图生成对应调用 ID 的未执行反馈并转入整理；运行 4.10 与动态策略测试，确认不会仅因多一次搜索意图丢掉合格证据，整理仍保留结构化响应能力且不降低来源要求
- [x] 4.12 先写一次纠错失败测试：已有资料下修格式不重搜、后置来源/轨迹纠正复用同一消息历史与台账、两类纠错共享一次额度、零成功工具时仅在剩余窗口内补齐首次必需调用、纠正仍无效拒绝；断言工具/模型/时间计数未重置，正常 `ambiguous/insufficient/conflict` 不进入格式重试，运行目标测试确认失败
- [x] 4.13 实现有限 `ToolStrategy.handle_errors` 与后置纠正的共享额度，错误反馈仅含安全字段路径/类别，不重新编译新研究或重建原始关键词请求；运行 4.12、4.10 及真实 Agent fake 测试，确认最多一次纠正、仍无效返回 `RESEARCH_AGENT_FAILED` 且没有第二次纠正或旧生成器调用
- [x] 4.14 先为请求状态隔离与清理补写失败测试，覆盖同主题并发请求、后续重试不能读取上次台账、成功/异常/取消释放工作区和消息引用，以及恶意网页/模型输出要求扩工具、伪造来源或泄露身份；运行目标测试确认新增生命周期缺口按预期失败
- [x] 4.15 完成请求级状态隔离、`finally` 清理和注入边界接线；运行 4.14 与原提示注入/正文分块测试，确认没有跨请求 checkpointer 或正文缓存、并发请求计数互不影响、原正文大小与不截页面开头的约束保持有效

## 5. 有据生成、事实校验与 API 持久化（TDD）

- [x] 5.1 先为研究验收编写失败测试，覆盖 `ready/ambiguous/insufficient/conflict`、最多三个候选解释、来源独立性、关键词 2～5 条、URL 1～5 条且包含原页面；实现确定性预筛并确认 `Harness Engineering` fixture 被解释为 AI agent 的环境/约束/反馈回路工程
- [x] 5.2 先为有据出题与 `GroundingReport` 编写失败测试，覆盖每关来源关联、不得引入无来源事实、逐字段事实检查、一次反馈重生成和重试后失败；调整 `ContentGenerator` 并实现结构化生成/校验，运行目标测试与现有 AI client 测试确认最多重生成一次且失败草稿不持久化
- [x] 5.3 先为完整策略服务编排编写失败测试，覆盖 grounded 的“内容安全 → 研究 Agent → 证据验收 → 生成 → 校验 → 持久化”、legacy 的“内容安全 → 旧生成器 → 持久化”、内容拦截时零外部调用、grounded 故障时零 legacy 调用、所有失败分支无数据库写入和取消/超时清理；实现服务与领域异常后运行测试确认顺序和原子性
- [x] 5.4 先扩展数据库/API 失败测试，覆盖 `input_type/source_input/retrieved_at/sources/source_ids`、URL 页面标题回退、legacy/旧记录默认值、GET 来源稳定、原七类业务错误与 `URL_REQUIRES_RESEARCH` 以及失败无半成品；运行目标测试确认尚未迁移时失败
- [x] 5.5 新增 Alembic 迁移，为 `learning_sessions` 添加 `input_type/source_input/retrieved_at/sources`，为 `levels` 添加 `source_ids`，并更新 SQLAlchemy 模型；对空库和含旧会话数据库执行 upgrade，确认旧数据仍可读取且未回填虚构来源
- [x] 5.6 扩展 `GameOut`、创建/读取和错误映射，在单一事务保存两种策略的统一结果与三关；运行 5.4 测试确认 grounded 七类错误及 legacy 的 `URL_REQUIRES_RESEARCH` 均保留稳定 HTTP 状态与 details，legacy/历史游戏稳定返回空来源且 grounded 来源不被改写
- [x] 5.7 先用可控单调时钟与取消 fake 写跨阶段预算失败测试，覆盖 30 秒停止新增检索、45 秒研究截止、85 秒总截止、提前结束研究可释放余量、每次生成为校验留 15 秒、一次反馈重生成与全部重试不重置总时限；区分外层检索窗口关闭与工具自身失败，并断言未校验草稿不会进入持久化，运行目标测试确认现有仅研究超时的实现失败
- [x] 5.8 在内容安全通过后的最外层 grounded 流水线实现绝对截止时间、研究整理/生成/校验预留和阶段原因分类，关闭 grounded SDK 隐藏重试叠加但不改 legacy 客户端；运行 5.7 与生成校验测试，确认所有子调用只用剩余时间、生成最多反馈重试一次、无预算时拒绝草稿而非跳过事实校验
- [x] 5.9 先写跨适配器→Agent→策略→HTTP 的错误与取消失败测试，覆盖设计第 12 节的 HTTP/code/reason 映射、服务器随机 `request_id`、既有歧义候选保留、正文/原始异常不外泄及显式客户端断连；断言 Tavily/页面错误不被改写为 `RESEARCH_AGENT_FAILED`，取消不会继续生成/提交，运行测试记录预期失败
- [x] 5.10 实现类型化领域错误透传、统一安全错误 details 和 HTTP 断连/取消处理；除设计第 12 节已确认的官方 Tavily 适配边界数字状态码兼容外，不以异常文案匹配类别，不吞掉取消；运行 5.9 及既有 API/生成流水线测试，确认 `SEARCH_UNAVAILABLE` 503、`PAGE_UNREADABLE` 422、研究失败 502、事实失败 502 等原契约不变，取消后工具/模型任务退出且无后台写入
- [x] 5.11 先为基础知识准入目录与失败签发条件写失败测试，覆盖完整命中“高情商聊天”、允许的既有规范化、未审核普通主题、网址及混合网址、新技术、版本/日期/最新要求、未知类别、未解歧义与冲突；按设计第 11 节逐类覆盖可签发失败及输入/认证/内容安全/取消/持久化/未知错误不可签发，运行目标测试确认缺失策略失败
- [x] 5.12 实现版本化、人工审核的稳定主题目录和确定性准入策略，只先覆盖已确认“高情商聊天”条目，不由模型扩表或用子串/模糊匹配授予许可；运行 5.11 测试，确认搜索失败或模型声称熟悉不能使未知输入获准，正常 grounded 输入范围不被该目录限制
- [x] 5.13 先为失败许可签发与验证写失败测试，覆盖默认 5 分钟、必需 claims/专用 audience/固定算法、随机许可 ID、原失败 ID、用户/主题绑定、策略版本、过期/篡改/换用户/换主题/缺关联、登录 token 与许可互用、密钥轮换或 legacy 下失效；断言 token 不含原主题/明文身份/研究内容，运行目标测试确认失败
- [x] 5.14 按设计实现用途隔离的 PyJWT 签名和带密钥绑定，只在已结束且未保存游戏的合格 grounded 失败中附加许可；运行 5.13、5.11 及错误响应测试，确认原 HTTP/code/reason 不被许可覆盖、未获准不含 token、basic 自身失败不递归签发、校验失败稳定返回 `BASIC_MODE_NOT_ALLOWED`
- [x] 5.15 先为结果模式与增量迁移写失败测试，覆盖 `generation_mode/verification_notice/basic_fallback_id`、旧无来源记录默认 legacy、真实有来源记录回填 grounded、basic 固定未核验提示与空来源/检索时间/关卡来源 ID、唯一许可 ID 及重读一致性；在 SQLite/迁移测试夹具上运行，确认新增契约或列缺失导致预期失败
- [x] 5.16 在现有 `0002_grounded_sources` 后新增独立 Alembic revision，更新 SQLAlchemy 与统一生成结果/GameOut 模型，不改已应用迁移；运行 5.15 和既有持久化/迁移测试，确认既有游戏不被误标 basic、不补造来源、新增字段兼容旧客户端且模式/提示来自数据库
- [x] 5.17 先为共享持久化与许可幂等写失败测试，覆盖同一有效许可重复提交返回已有完整游戏、并发唯一冲突回滚、不同用户不能读取竞争结果，以及保存会话/关卡/来源各阶段失败或取消；断言最多一局完整游戏、没有半成品、外部生成阶段不持数据库行锁，运行目标测试确认缺失行为失败
- [x] 5.18 完成 grounded/legacy/basic 统一结果的原子写入与 `basic_fallback_id` 唯一冲突处理，支持在完成授权验证后安全重放已有结果；运行 5.17 和原事务测试，确认失败全部回滚、提交前检查取消、数据库只存随机许可 ID 而不存 token 或研究正文
- [x] 5.19 先为 `POST /api/v1/games/basic` 写 HTTP/服务失败测试，覆盖已有认证、80 字关键词边界、严格额外字段拒绝、`acknowledge_unverified` 必须为布尔真值、许可/用户/原主题/当前策略再验证、再次内容安全拒绝或不可用、完整三关成功、输出无效/生成不可用/85 秒独立预算失败及有效许可重放；断言未通过准入零生成、basic 零 Tavily/Agent/联网校验调用，运行目标测试确认失败
- [x] 5.20 实现独立基础知识端点与 `BasicKnowledgeService`，按设计顺序校验后复用原关键词生成器及共享事务，返回持久化 basic 标识与“未经联网核验”；运行 5.19、5.13、5.17 及原创建/答题测试，确认常规请求不能靠额外模式字段走 basic、单个用户选择不改变全局模式、重放完整结果不重复调用生成器且失败不伪装成功
- [x] 5.21 增加三条路径的跨服务集成回归，验证“grounded 失败保持失败 → 附加许可 → 不选择则无新调用 → 合格用户显式另发 basic 请求才成功”，并与普通 grounded、部署 legacy 并发执行；以所有断言通过为标准，确认原失败与后续请求 ID 不同、许可不进入模型提示、模式/来源/内容安全及失败原子性始终符合规格
- [x] 5.22 增加并运行 MySQL 8 集成测试，覆盖原 JSON/URL/时区测试及本次模式字段、已有来源回填、许可唯一索引、多连接并发重放、事务失败/取消和迁移往返；仅在可丢弃测试库执行 `upgrade → downgrade → upgrade`，以 `pytest -m mysql` 无跳过且全部通过为完成标准，没有 MySQL 环境不得宣称完成

## 6. 前端原型确认与实现

- [x] 6.1 在 `design/` 补齐长 URL 输入、搜索/整页读取两种加载状态、grounded 七类业务错误、`URL_REQUIRES_RESEARCH`、legacy/旧游戏无来源状态和通关参考资料区原型，严格延续现有设计稿视觉规则；向用户展示并以明确确认作为继续相关 TSX/SCSS 的门禁
- [x] 6.2 扩展前端 `Game/SourceReference/ApiError` 类型、关键词/URL 轻量判定、2048 字符输入和请求错误解析，保留 `code/details` 并区分 HTTP 业务错误与无响应网络错误；增加数据层测试并运行 `pnpm typecheck`、`pnpm lint`
- [x] 6.3 完整读取现有 UI 资源，在 `design/` 沿用已确认视觉规范补齐获准失败的重试联网/基础知识操作、选择前告知、未获准/无 HTTP 响应、许可过期、basic 生成失败，以及创建/答题/通关/重进的未核验标识原型；用浏览器逐状态检查并提供可查看原型与截图，不自行增加布局风格或虚构阶段进度
- [x] 6.4 向用户展示 6.3 原型并取得对新增交互/文案/标识的明确确认，记录确认范围后才勾选；未确认或要求修改时不得开始对应 TSX/SCSS，实现计划确认及旧 6.1 确认不能替代此门禁
- [x] 6.5 先为前端新 API 契约与许可生命周期写失败测试，覆盖 `request_id/reason/fallback`、`generation_mode/verification_notice`、旧响应缺字段、无响应不推断许可、只有显式选择才提交 basic、换输入/账号/离开/重试联网清理许可、旧异步响应不恢复过期入口，以及两种创建请求均为 `timeout: 90000`；运行数据层测试确认未实现分支失败
- [x] 6.6 扩展 API 类型、基础知识独立请求和页面内存态管理，保留原输入及 HTTP 业务错误，不持久存储或记录 token，不在失败时自动预请求 basic；运行 6.5 和既有请求/错误解析测试及 `pnpm typecheck`，确认严格布尔同意字段仅在用户选择后提交，网络错误不伪造后端许可
- [x] 6.7 通过 6.4 后按原型实现输入、准确加载文案、业务失败和基础知识入口/过期/重试交互，提交期间避免重复点击；注入全部相关 HTTP 错误与断网状态验证 UI，确认八类原错误仍可操作、未经许可无按钮、选择前可见告知、重试保留输入且不伪造“查到一半”等进度
- [ ] 6.8 按已确认原型完成通关参考资料区，以及从持久化模式/提示渲染的 basic 创建成功、答题、通关与重进标识；在 H5/微信端验证 grounded 来源日期/标题/域名/获取方式，微信复制规范 URL、H5 打开链接，legacy/旧游戏无空资料容器，basic 始终显示未经联网核验而不显示联网标识
- [ ] 6.9 做前后端联调回归，逐一覆盖获准与未获准失败、明确选择/不选择、换主题后的迟到响应、过期许可、basic 失败重试、重复点击及游戏重进；以浏览器和微信开发者工具/真机的观察与请求记录为证据，确认没有自动降级、跨主题许可复用或持久标识丢失，记录中不保留真实凭据
- [ ] 6.10 运行前端数据层测试、`pnpm typecheck`、`pnpm lint`、`pnpm build:weapp` 和 `pnpm build:h5`；确认全部通过且构建产物无 Tavily/DeepSeek key、兜底许可、完整网页正文或前端直连 Tavily 的代码，未完成双端验证不得只凭 H5 构建宣称小程序可用

## 7. 安全、可观测性与交付验证

- [x] 7.1 先编写跨研究/生成/准入/basic 的日志与指标失败测试，覆盖模式、阶段、耗时、工具与模型计数、物理重试、结果/正文字符/来源数、reason、校验问题、准入结果、随机 `request_id` 和 `parent_request_id`；在异常链及客户端日志中注入虚构敏感标记，断言没有 key/用户 ID/OpenID/JWT/许可/完整输入或其易枚举哈希/完整 URL/正文/Agent 消息/供应商响应，运行目标测试确认缺口失败
- [x] 7.2 实现最小化结构化日志与低基数指标，默认禁用原始模型/工具载荷追踪，统一处理异常、访问日志和客户端埋点的敏感字段；运行 7.1 及取消/生命周期测试，确认可以关联原失败与 basic 而不使用身份或内容，指标标签不包含 request ID/主题/用户值
- [x] 7.3 在研究完整依赖环境运行全量后端 `pytest --cov` 并检查分支覆盖率不低于项目 85% 门槛，同时运行前端测试与 6.10 构建检查；Tavily、DeepSeek、DNS 和时间均隔离，不消耗真实配额，缺失环境/跳过集成测试需单独报告，不能计作通过
- [ ] 7.4 进行跨 HTTP 与 UI 的故障注入验收，覆盖 Tavily 认证/限流/网络/超时、网页不可读、Agent 零工具/无效结论/预算用尽、研究及生成校验截止、事实校验失败、持久化失败和断连；确认原错误类别和请求关联可诊断、没有半成品或后台续写、同一 grounded 请求零旧生成器调用，只有后端获准且用户另行选择才走 basic
- [x] 7.5 在全新无 research extra 环境验证 legacy 启动/关键词创建/URL 拒绝及原游戏读取，在完整开发环境显式切换 grounded→legacy→grounded 验证全局模式隔离；确认 legacy 不读 Tavily key、不装配研究 Agent、不签发/接受 basic 许可，而已存在 basic 的核验提示在切换和兼容回滚中仍保留，完成后恢复原开发配置
- [x] 7.6 使用已轮换的开发凭证执行一次受控真实模型能力检查，验证配置模型完整完成“Tool Call→工具结果→`ResearchConclusion`”并由服务端接受；记录版本、阶段与安全结果，`bind_tools` 或 fake 成功不算真实验证，能力不足/认证异常时报告且不静默换模型、改生产或无限重试
- [ ] 7.7 在开发 grounded 模式执行受控真实端到端回归，至少覆盖“高情商聊天”、`Harness Engineering`、简单摘要、复杂全文、普通公开 URL、含表格复杂 URL、中国城市和国际技术主题；核查实际动态参数、收敛轨迹、来源数量/原页面、三关事实校验及总耗时。记录成功与失败实测结果，不输出正文/身份/凭据，不以 fake 测试代替真实链路修复证据
- [ ] 7.8 在开发环境受控注入研究失败，通过已确认 UI 验证“高情商聊天”只有主动选择才另发 basic 请求、再次内容安全通过后完整生成且重进保留未核验提示；同时验证网址、新技术/版本及未知主题即使直接请求端点也不能兜底。完成后恢复故障注入与原开发配置，结果须包含实际操作与无半成品检查，不能把自动触发视作人工选择
- [ ] 7.9 检查 `git diff`、待提交文件、测试记录和前后端产物的秘密/正文/身份缺失，运行 OpenSpec 严格校验并整理 TDD、MySQL、双端 UI 与真实回归证据；更新开发/生产配置差异、预算、错误排查、保守准入目录、全局 legacy 切换及保留 basic 提示的兼容回滚说明。全部门禁通过后报告可交付状态；本任务不包含生产部署、推送、主规格同步或归档，这些动作另按用户授权及对应流程进行
